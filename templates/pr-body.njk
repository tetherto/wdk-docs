## API Reference Update

This PR is maintained by automation. Per-version release notes, note-only entries, and ripple warnings live in the sticky comment.

- Trigger: `{{ eventName }}`
- Module scope: `{{ moduleLabel }}`
- Versions processed this run: {{ run.attempts }}
- Docs changed: {{ run.docChanged }}
- Note-only versions: {{ run.noteOnly }}
- Failures: {{ run.failures }}
- Ripple warnings: {{ run.rippleWarnings }}
{% if run.lastTag %}- Latest processed tag: `{{ run.lastTag }}`{% endif %}
{% if failedSteps.length %}

### Failed Versions
{% for step in failedSteps %}
- `{{ step.key }}` at `{{ step.toTag or 'unknown' }}`
{% endfor %}
{% endif %}
