---
title: API Reference
description: Complete API documentation for @tetherto/wdk-wallet-evm
docType: reference
schemaType: APIReference
icon: Code
---

{/* AUTO-GENERATED — DO NOT EDIT. Run scripts/api-ref-gen to regenerate. */}

# API Reference

## Table of Contents

| Class | Description | Methods |
|-------|-------------|---------|
| [WalletManagerEvm](#walletmanagerevm) | WalletManagerEvm class | 6 methods |
| [WalletAccountEvm](#walletaccountevm) | WalletAccountEvm class | 16 methods |
| [WalletAccountReadOnlyEvm](#walletaccountreadonlyevm) | WalletAccountReadOnlyEvm class | 9 methods |

---

## WalletManagerEvm

### Constructor [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-manager-evm.js#L52)

```javascript
new WalletManagerEvm(seed, config?)
```

**Parameters:**

- `seed` (string \| Uint8Array<ArrayBufferLike>): The wallet's [BIP-39](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki) seed phrase.
- `config` (EvmWalletConfig, optional): The configuration object.

### Methods

| Method | Description | Returns | Throws |
|--------|-------------|---------|--------|
| `dispose()` | Disposes all the wallet accounts, erasing their private keys from the memory. | `void` | - |
| `getAccount(index)` | Returns the wallet account at a specific index (see [BIP-44](https://github.com/bitcoin/bips/blob/master/bip-0044.mediawiki)). | `Promise<WalletAccountEvm>` | - |
| `getAccountByPath(path)` | Returns the wallet account at a specific BIP-44 derivation path. | `Promise<WalletAccountEvm>` | - |
| `getFeeRates()` | Returns the current fee rates. | `Promise<FeeRates>` | - |
| `(static) getRandomSeedPhrase(wordCount)` | Returns a random [BIP-39](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki) seed phrase. | `string` | - |
| `(static) isValidSeedPhrase(seedPhrase)` | Checks if a seed phrase is valid. | `boolean` | - |

#### `dispose()`

Disposes all the wallet accounts, erasing their private keys from the memory.

**Returns:** `void`

#### `getAccount(index)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-manager-evm.js#L87)

Returns the wallet account at a specific index (see [BIP-44](https://github.com/bitcoin/bips/blob/master/bip-0044.mediawiki)).

**Parameters:**

- `index` (number, optional): The index of the account to get (default: 0).

**Returns:** `Promise<WalletAccountEvm>` - The account.

**Example:**

```javascript
// Returns the account with derivation path m/44'/60'/0'/0/1
const account = await wallet.getAccount(1);
```

#### `getAccountByPath(path)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-manager-evm.js#L100)

Returns the wallet account at a specific BIP-44 derivation path.

**Parameters:**

- `path` (string): The derivation path (e.g. "0'/0/0").

**Returns:** `Promise<WalletAccountEvm>` - The account.

**Example:**

```javascript
// Returns the account with derivation path m/44'/60'/0'/0/1
const account = await wallet.getAccountByPath("0'/0/1");
```

#### `getFeeRates()` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-manager-evm.js#L115)

Returns the current fee rates.

**Returns:** `Promise<FeeRates>` - The fee rates (in weis).

#### `getRandomSeedPhrase(wordCount)` (static)

Returns a random [BIP-39](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki) seed phrase.

**Parameters:**

- `wordCount` (12 \| 24, optional): The number of words in the seed phrase.

**Returns:** `string` - The seed phrase.

#### `isValidSeedPhrase(seedPhrase)` (static)

Checks if a seed phrase is valid.

**Parameters:**

- `seedPhrase` (string): The seed phrase.

**Returns:** `boolean` - True if the seed phrase is valid.

### Properties

| Property | Type | Description |
|----------|------|-------------|
| `seed` | `Uint8Array` | The seed phrase of the wallet. |

---

## WalletAccountEvm

### Constructor [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L57)

```javascript
new WalletAccountEvm(seed, path, config?)
```

**Parameters:**

- `seed` (string \| Uint8Array<ArrayBufferLike>): The wallet's [BIP-39](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki) seed phrase.
- `path` (string): The BIP-44 derivation path (e.g. "0'/0/0").
- `config` (EvmWalletConfig, optional): The configuration object.

### Methods

| Method | Description | Returns | Throws |
|--------|-------------|---------|--------|
| `approve(options)` | Approves a specific amount of tokens to a spender. | `Promise<TransactionResult>` | Yes |
| `dispose()` | Disposes the wallet account, erasing the private key from the memory. | `void` | - |
| `getAddress()` | Returns the account's address. | `Promise<string>` | - |
| `getAllowance(token, spender)` | Returns the current allowance for the given token and spender. | `Promise<bigint>` | - |
| `getBalance()` | Returns the account's eth balance. | `Promise<bigint>` | - |
| `getTokenBalance(tokenAddress)` | Returns the account balance for a specific token. | `Promise<bigint>` | - |
| `getTransactionReceipt(hash)` | Returns a transaction's receipt. | `Promise<TransactionReceipt>` | - |
| `quoteSendTransaction(tx)` | Quotes the costs of a send transaction operation. | `Promise<Omit<TransactionResult, "hash">>` | - |
| `quoteTransfer(options)` | Quotes the costs of a transfer operation. | `Promise<Omit<TransferResult, "hash">>` | - |
| `sendTransaction(tx)` | Sends a transaction. | `Promise<TransactionResult>` | - |
| `sign(message)` | Signs a message. | `Promise<string>` | - |
| `signTypedData(typedData)` | Signs typed data according to EIP-712. | `Promise<string>` | - |
| `toReadOnlyAccount()` | Returns a read-only copy of the account. | `Promise<WalletAccountReadOnlyEvm>` | - |
| `transfer(options)` | Transfers a token to another address. | `Promise<TransferResult>` | - |
| `verify(message, signature)` | Verifies a message's signature. | `Promise<boolean>` | - |
| `verifyTypedData(typedData, signature)` | Verifies a typed data signature. | `Promise<boolean>` | - |

#### `approve(options)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L196)

Approves a specific amount of tokens to a spender.

**Parameters:**

- `options` (ApproveOptions): The approve options.

**Returns:** `Promise<TransactionResult>` - The transaction’s result.

**Throws:** If trying to approve usdts on ethereum with allowance not equal to zero (due to the usdt allowance reset requirement).

#### `dispose()` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L239)

Disposes the wallet account, erasing the private key from the memory.

**Returns:** `void`

#### `getAddress()`

Returns the account's address.

**Returns:** `Promise<string>` - The account's address.

#### `getAllowance(token, spender)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L196)

Returns the current allowance for the given token and spender.

**Parameters:**

- `token` (string): The token’s address.
- `spender` (string): The spender’s address.

**Returns:** `Promise<bigint>` - The allowance.

#### `getBalance()` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L103)

Returns the account's eth balance.

**Returns:** `Promise<bigint>` - The eth balance (in weis).

#### `getTokenBalance(tokenAddress)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L121)

Returns the account balance for a specific token.

**Parameters:**

- `tokenAddress` (string): The smart contract address of the token.

**Returns:** `Promise<bigint>` - The token balance (in base unit).

#### `getTransactionReceipt(hash)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L182)

Returns a transaction's receipt.

**Parameters:**

- `hash` (string): The transaction's hash.

**Returns:** `Promise<TransactionReceipt>` - – The receipt, or null if the transaction has not been included in a block yet.

#### `quoteSendTransaction(tx)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L141)

Quotes the costs of a send transaction operation.

**Parameters:**

- `tx` (EvmTransaction): The transaction.

**Returns:** `Promise<Omit<TransactionResult, "hash">>` - The transaction's quotes.

#### `quoteTransfer(options)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L164)

Quotes the costs of a transfer operation.

**Parameters:**

- `options` (TransferOptions): The transfer's options.

**Returns:** `Promise<Omit<TransferResult, "hash">>` - The transfer's quotes.

#### `sendTransaction(tx)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L150)

Sends a transaction.

**Parameters:**

- `tx` (EvmTransaction): The transaction.

**Returns:** `Promise<TransactionResult>` - The transaction's result.

#### `sign(message)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L130)

Signs a message.

**Parameters:**

- `message` (string): The message to sign.

**Returns:** `Promise<string>` - The message's signature.

#### `signTypedData(typedData)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L140)

Signs typed data according to EIP-712.

**Parameters:**

- `typedData` (TypedData): The typed data to sign.

**Returns:** `Promise<string>` - The typed data signature.

#### `toReadOnlyAccount()` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L230)

Returns a read-only copy of the account.

**Returns:** `Promise<WalletAccountReadOnlyEvm>` - The read-only account.

#### `transfer(options)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L171)

Transfers a token to another address.

**Parameters:**

- `options` (TransferOptions): The transfer's options.

**Returns:** `Promise<TransferResult>` - The transfer's result.

#### `verify(message, signature)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L215)

Verifies a message's signature.

**Parameters:**

- `message` (string): The original message.
- `signature` (string): The signature to verify.

**Returns:** `Promise<boolean>` - True if the signature is valid.

#### `verifyTypedData(typedData, signature)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L229)

Verifies a typed data signature.

**Parameters:**

- `typedData` (TypedData): The typed data to verify.
- `signature` (string): The signature to verify.

**Returns:** `Promise<boolean>` - True if the signature is valid.

### Properties

| Property | Type | Description |
|----------|------|-------------|
| `address` | `string` | The account's address. |
| `index` | `number` | The derivation path's index of this account. |
| `keyPair` | `KeyPair` | The account's key pair. |
| `path` | `string` | The derivation path of this account (see [BIP-44](https://github.com/bitcoin/bips/blob/master/bip-0044.mediawiki)). |

---

## WalletAccountReadOnlyEvm

### Constructor [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L63)

```javascript
new WalletAccountReadOnlyEvm(address, config?)
```

**Parameters:**

- `address` (string): The account's address.
- `config` (Omit<EvmWalletConfig, "transferMaxFee">, optional): The configuration object.

### Methods

| Method | Description | Returns | Throws |
|--------|-------------|---------|--------|
| `getAddress()` | Returns the account's address. | `Promise<string>` | - |
| `getAllowance(token, spender)` | Returns the current allowance for the given token and spender. | `Promise<bigint>` | - |
| `getBalance()` | Returns the account's eth balance. | `Promise<bigint>` | - |
| `getTokenBalance(tokenAddress)` | Returns the account balance for a specific token. | `Promise<bigint>` | - |
| `getTransactionReceipt(hash)` | Returns a transaction's receipt. | `Promise<TransactionReceipt>` | - |
| `quoteSendTransaction(tx)` | Quotes the costs of a send transaction operation. | `Promise<Omit<TransactionResult, "hash">>` | - |
| `quoteTransfer(options)` | Quotes the costs of a transfer operation. | `Promise<Omit<TransferResult, "hash">>` | - |
| `verify(message, signature)` | Verifies a message's signature. | `Promise<boolean>` | - |
| `verifyTypedData(typedData, signature)` | Verifies a typed data signature. | `Promise<boolean>` | - |

#### `getAddress()`

Returns the account's address.

**Returns:** `Promise<string>` - The account's address.

#### `getAllowance(token, spender)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L196)

Returns the current allowance for the given token and spender.

**Parameters:**

- `token` (string): The token’s address.
- `spender` (string): The spender’s address.

**Returns:** `Promise<bigint>` - The allowance.

#### `getBalance()` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L103)

Returns the account's eth balance.

**Returns:** `Promise<bigint>` - The eth balance (in weis).

#### `getTokenBalance(tokenAddress)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L121)

Returns the account balance for a specific token.

**Parameters:**

- `tokenAddress` (string): The smart contract address of the token.

**Returns:** `Promise<bigint>` - The token balance (in base unit).

#### `getTransactionReceipt(hash)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L182)

Returns a transaction's receipt.

**Parameters:**

- `hash` (string): The transaction's hash.

**Returns:** `Promise<TransactionReceipt>` - – The receipt, or null if the transaction has not been included in a block yet.

#### `quoteSendTransaction(tx)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L141)

Quotes the costs of a send transaction operation.

**Parameters:**

- `tx` (EvmTransaction): The transaction.

**Returns:** `Promise<Omit<TransactionResult, "hash">>` - The transaction's quotes.

#### `quoteTransfer(options)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L164)

Quotes the costs of a transfer operation.

**Parameters:**

- `options` (TransferOptions): The transfer's options.

**Returns:** `Promise<Omit<TransferResult, "hash">>` - The transfer's quotes.

#### `verify(message, signature)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L215)

Verifies a message's signature.

**Parameters:**

- `message` (string): The original message.
- `signature` (string): The signature to verify.

**Returns:** `Promise<boolean>` - True if the signature is valid.

#### `verifyTypedData(typedData, signature)` [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L229)

Verifies a typed data signature.

**Parameters:**

- `typedData` (TypedData): The typed data to verify.
- `signature` (string): The signature to verify.

**Returns:** `Promise<boolean>` - True if the signature is valid.

### Properties

| Property | Type | Description |
|----------|------|-------------|
| `address` | `string` | The account's address. |

---

## Types

### ApproveOptions [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-evm.js#L39)

| Property | Type | Description |
|----------|------|-------------|
| `amount` | `number \| bigint` | The amount of tokens to approve to the spender. |
| `spender` | `string` | The spender’s address. |
| `token` | `string` | The address of the token to approve. |

### EvmTransaction [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L40)

| Property | Type | Description |
|----------|------|-------------|
| `data?` | `string` | The transaction's data in hex format. |
| `gasLimit?` | `number \| bigint` | The maximum amount of gas this transaction is permitted to use. |
| `gasPrice?` | `number \| bigint` | The price (in wei) per unit of gas this transaction will pay. |
| `maxFeePerGas?` | `number \| bigint` | The maximum price (in wei) per unit of gas this transaction will pay for the combined [EIP-1559](https://eips.ethereum.org/EIPS/eip-1559) block's base fee and this transaction's priority fee. |
| `maxPriorityFeePerGas?` | `number \| bigint` | The price (in wei) per unit of gas this transaction will allow in addition to the [EIP-1559](https://eips.ethereum.org/EIPS/eip-1559) block's base fee to bribe miners into giving this transaction priority. This is included in the maxFeePerGas, so this will not affect the total maximum cost set with maxFeePerGas. |
| `to` | `string` | The transaction's recipient. |
| `value` | `number \| bigint` | The amount of ethers to send to the recipient (in weis). |

### EvmWalletConfig [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L51)

| Property | Type | Description |
|----------|------|-------------|
| `provider?` | `string \| Eip1193Provider` | The url of the rpc provider, or an instance of a class that implements eip-1193. |
| `transferMaxFee?` | `number \| bigint` | The maximum fee amount for transfer operations. |

### TypedData [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L33)

| Property | Type | Description |
|----------|------|-------------|
| `domain` | `TypedDataDomain` | The domain separator. |
| `message` | `Record<string, unknown>` | The message data. |
| `types` | `Record<string, TypedDataField[]>` | The type definitions. |

### EvmTransactionReceipt [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/index.js#L16)

### FeeRates

### KeyPair

### TransactionResult

### TransferOptions

### TransferResult

### TypedDataDomain [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L29)

### TypedDataField [source](https://github.com/tetherto/wdk-wallet-evm/blob/90fccc9441fc6e162697b1c483ec0dd58cc0ac93/src/wallet-account-read-only-evm.js#L30)
